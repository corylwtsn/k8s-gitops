apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-code-git
  namespace: agents
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: freenas-nfs-csi
  resources:
    requests:
      storage: 20Gi
