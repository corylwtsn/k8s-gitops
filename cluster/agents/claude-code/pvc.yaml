apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-code-home
  namespace: agents
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: freenas-nfs-csi
  resources:
    requests:
      storage: 20Gi
