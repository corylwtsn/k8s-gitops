---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app planka
spec:
  driftDetection:
    mode: enabled
  dependsOn:
    - name: planka-postgres
  chart:
    spec:
      chart: *app
      version: 2.1.0
      sourceRef:
        kind: HelmRepository
        name: planka-charts
        namespace: flux-system
  interval: 30m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
      remediateLastFailure: true
  values:
    replicaCount: 1
    image:
      repository: ghcr.io/plankanban/planka
      pullPolicy: IfNotPresent
      tag: 2.2.1@sha256:125f45330853210c678fed1132bedc0912b4a11a27779b79802fbca108e069e2

    existingSecretkeySecret: planka
    existingAdminCredsSecret: planka
    existingDburlSecret: planka-postgres-cluster-app

    persistence:
      enabled: true
      existingClaim: planka-data

    postgresql:
      enabled: false

    oidc:
      enabled: true
      existingSecret: "planka"
      issuerUrl: "https://auth.wat.sn/application/o/planka/"
      admin:
        rolesAttribute: groups
        roles:
          - planka-admin
